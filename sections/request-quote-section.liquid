{{ 'section-contact-form.css' | asset_url | stylesheet_tag }}

{%- style -%}
.contact__form--wrapper.color-background-3{
  background:#fff;
}
.text__area{
  padding:0px 6px !important;
}
#product_Box_0 .action_btns .deleteBox{
  opacity:0;
}
.counter{
  font-size:20px;
}
#delete{
  width: 25px;
  padding: 0px 3px;
  margin: 0px 15px;
  cursor:pointer;
}
.action_btns{
  display:flex;
  align-items:self-end;
}
input,select,textarea{
   -webkit-box-shadow: rgba(50, 50, 93, 0.14902) 0px 1px 3px, rgba(0, 0, 0, 0.0196078) 0px 1px 0px;
    box-shadow: rgba(50, 50, 93, 0.14902) 0px 1px 3px, rgba(0, 0, 0, 0.0196078) 0px 1px 0px;
    padding:2px 4px;
}
input::-ms-input-placeholder,select::placeholder,textarea::placeholder { 
  color: #6d6d6d !important;
  padding:2px 5px;
  font-weight:normal !important;
}
input::placeholder,select::placeholder {
  color: #6d6d6d !important;
  padding:2px 5px;
  font-weight:normal !important;
}
select{
  color:#6d6d6d !important;
}
.customize{
  margin-top:12px;
}
@media(min-width:769px){
  
.customize label{
  font-size:20px;
  font-weight:bold;
}
  .customize input{
width:18px;
height:18px;
}
 .box_inner {
    margin: 35px 0px;
    display: flex;
    grid-template-columns: repeat(3,1fr);
    align-items: end;
    gap: 10px;
    flex-wrap: wrap;
 }
  .box_inner .input__field_form{
    width:30%;
  }
  .top_form_fields{
    display: grid;
    grid-template-columns: repeat(3,1fr);
    gap: 10px;
    align-items: end;
  }

}
  #add_another_product,.contact__button button{
    background: #80cd2c;
    padding: 8px 12px;
    color: #fff;
    text-transform: capitalize;
    margin-top:15px;
    font-size:23px;
    cursor:pointer;
  }
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.mobile_padding_top }}px;
    padding-bottom: {{ section.settings.mobile_padding_bottom }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

{% liquid
  case section.settings.button_type
    when 'primary'
      assign button_class = 'button button--medium '
    when 'secondary'
      assign button_class = 'button button--secondary'
    when 'icon'
      assign button_class = 'link with--icon'
    else
      assign button_class = 'link'
  endcase

  if section.settings.full_width
    assign content_column_class = 'col-12'
    assign form_wrapper_class = 'col-12'
  else
    assign content_column_class = 'col-md-4 col-lg-6 col-12'
    assign form_wrapper_class = 'col-md-8  col-lg-6 col-12'
  endif
%}

<div class="contact__section section-{{ section.id }}-padding">
  <div class="container">
    <div class="row contact__form--container {% if section.settings.full_width %} full--width-section {% else %} half--width-section {% endif %}">
      <div class="{{ content_column_class }}">
        {%- if section.settings.heading_tag == 'h1' -%}
          <h1 class="contact__heading {{ section.settings.heading_size }}">{{ section.settings.heading | escape }}</h1>
        {%- else -%}
          <h2 class="contact__heading {{ section.settings.heading_size }}">{{ section.settings.heading | escape }}</h2>
        {%- endif -%}

        {%- if section.blocks.size > 0 -%}
          <div class="contact__list--wrapper">
            {%- for block in section.blocks -%}
              {%- case block.type -%}
                {%- when 'contact_list' -%}
                  <div class="contact__list--item" {{ block.shopify_attributes }}>
                    <span class="h4 contact__list--item-heading">{{ block.settings.heading }}</span>
                    <div class="contact__list--item-content">{{ block.settings.text }}</div>
                  </div>
              {%- endcase -%}
            {%- endfor -%}
          </div>
        {%- endif -%}
      </div>
      <div class="{{ form_wrapper_class }}">
        <div class="contact__form--wrapper color-{{ section.settings.color_scheme }}">
          <div class="contact__form--header">
            {%- if section.settings.form_heading_tag == 'h2' -%}
              <h2 class="contact__form--heading {{ section.settings.form_heading_size }}">
                {{ section.settings.form_heading }}
              </h2>
            {%- else -%}
              <h3 class="contact__form--heading {{ section.settings.form_heading_size }}">
                {{ section.settings.form_heading }}
              </h3>
            {%- endif -%}
            <p class="contact__form--text">{{ section.settings.subheading }}</p>
          </div>
         {%- form 'contact', id: 'ContactForm' -%}
    {%- if form.posted_successfully? -%}
        <div class="form-status form-status-list form__message" tabindex="-1" autofocus>
            {% render 'icon-success' %}
            {{ 'templates.contact.form.post_success' | t }}
        </div>
    {%- elsif form.errors -%}
        <div class="form__message">
            <h2 class="form-status caption-large" role="alert" tabindex="-1" autofocus>
                {% render 'icon-error' %}
                {{ 'templates.contact.form.error_heading' | t }}
            </h2>
        </div>
        <ul class="form-status-list caption-large" role="list">
            <li>
                <a href="#ContactForm-email" class="link">
                    {{ form.errors.translated_fields.email | capitalize }}
                    {{ form.errors.messages.email }}
                </a>
            </li>
        </ul>
    {%- endif -%}
<div class="top_form_fields">
    <!-- Existing Name Field -->
    <div class="input__field_form">
        <input
            class="input__field h6"
            autocomplete="name"
            type="text"
            id="ContactForm-name"
            name="contact[{{ 'templates.contact.form.name' | t }}]"
            value="{% if form.name %}{{ form.name }}{% elsif customer %}{{ customer.name }}{% endif %}"
            placeholder="{{ 'templates.contact.form.name' | t }}"
        >
        <label class="visually-hidden" for="ContactForm-name">{{ 'templates.contact.form.name' | t }}</label>
    </div>

    <!-- Existing Email Field -->
    <div class="input__field_form field--with-error">
        <input
            autocomplete="email"
            type="email"
            id="ContactForm-email"
            class="input__field h6"
            name="contact[email]"
            spellcheck="false"
            autocapitalize="off"
            value="{% if form.email %}{{ form.email }}{% elsif customer %}{{ customer.email }}{% endif %}"
            aria-required="true"
            {% if form.errors contains 'email' %}
                aria-invalid="true"
                aria-describedby="ContactForm-email-error"
            {% endif %}
            placeholder="{{ 'templates.contact.form.email' | t }}"
        >
        <label class="visually-hidden" for="ContactForm-email">
            {{- 'templates.contact.form.email' | t }}
            <span aria-hidden="true">*</span>
        </label>
        {%- if form.errors contains 'email' -%}
            <small class="contact__field-error" id="ContactForm-email-error">
                <span class="visually-hidden">{{ 'accessibility.error' | t }}</span>
                <span class="form__message">
                    {%- render 'icon-error' -%}
                    {{- form.errors.translated_fields.email | capitalize }}
                    {{ form.errors.messages.email -}}
                </span>
            </small>
        {%- endif -%}
    </div>

    <!-- Add New Fields -->

    <!-- PO Number -->
    <div class="input__field_form">
        <input
            class="input__field h6"
            type="text"
            id="ContactForm-po-number"
            name="contact[po number]"
            value="{% if form.po_number %}{{ form.po_number }}{% endif %}"
            placeholder="Po Number"
        >
        <label class="visually-hidden" for="ContactForm-po-number">Po Number</label>
    </div>

    <!-- Organization -->
    <div class="input__field_form">
        <input
            class="input__field h6"
            type="text"
            id="ContactForm-organization"
            name="contact[organization name]"
            value="{% if form.organization %}{{ form.organization }}{% endif %}"
            placeholder="Organization/Team Name"
        >
        <label class="visually-hidden" for="ContactForm-organization">Organization Team Name</label>
    </div>

    <!-- Organization Type -->
    <div class="input__field_form">
        <input
            class="input__field h6"
            type="text"
            id="ContactForm-organization-type"
            name="contact[organization type]"
            value="{% if form.organization_type %}{{ form.organization_type }}{% endif %}"
            placeholder="Organization Type/Team optional"
        >
        <label class="visually-hidden" for="ContactForm-organization-type">Organiztion Type</label>
    </div>

    <!-- Address -->
    <div class="input__field_form">
        <input
            class="input__field h6"
            type="text"
            id="ContactForm-address"
            name="contact[address]"
            value="{% if form.address %}{{ form.address }}{% endif %}"
            placeholder="Address"
        >
        <label class="visually-hidden" for="ContactForm-address">Address</label>
    </div>

    <!-- City -->
    <div class="input__field_form">
        <input
            class="input__field h6"
            type="text"
            id="ContactForm-city"
            name="contact[city]"
            value="{% if form.city %}{{ form.city }}{% endif %}"
            placeholder="City"
        >
        <label class="visually-hidden" for="ContactForm-city">City</label>
    </div>

    <!-- State -->
    <div class="input__field_form">
        <input
            class="input__field h6"
            type="text"
            id="ContactForm-state"
            name="contact[state]"
            value="{% if form.state %}{{ form.state }}{% endif %}"
            placeholder="State"
        >
        <label class="visually-hidden" for="ContactForm-state">State</label>
    </div>

    <!-- Zip Code -->
    <div class="input__field_form">
        <input
            class="input__field h6"
            type="text"
            id="ContactForm-zip-code"
            name="contact[zip code]"
            value="{% if form.zip_code %}{{ form.zip_code }}{% endif %}"
            placeholder="Zip Code"
        >
        <label class="visually-hidden" for="ContactForm-zip-code">Zip Code</label>
    </div>
    <div class="input__field_form">
        <input
            type="tel"
            id="ContactForm-phone"
            class="input__field h6"
            autocomplete="tel"
            name="contact[{{ 'templates.contact.form.phone' | t }}]"
            pattern="[0-9\-]*"
            value="{% if form.phone %}{{ form.phone }}{% elsif customer %}{{ customer.phone }}{% endif %}"
            placeholder="{{ 'templates.contact.form.phone' | t }}"
        >
        <label class="visually-hidden" for="ContactForm-phone">{{ 'templates.contact.form.phone' | t }}</label>
    </div>   
</div>
          <br>
    <h2>PRODUCTS</h2>
     <div class="product_box" id='product_Box_0'>
       <div class="action_btns">
    <span class="counter">Item 1</span>
     <span class="deleteBox">
       <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" id="delete"><path d="M24.2,12.193,23.8,24.3a3.988,3.988,0,0,1-4,3.857H12.2a3.988,3.988,0,0,1-4-3.853L7.8,12.193a1,1,0,0,1,2-.066l.4,12.11a2,2,0,0,0,2,1.923h7.6a2,2,0,0,0,2-1.927l.4-12.106a1,1,0,0,1,2,.066Zm1.323-4.029a1,1,0,0,1-1,1H7.478a1,1,0,0,1,0-2h3.1a1.276,1.276,0,0,0,1.273-1.148,2.991,2.991,0,0,1,2.984-2.694h2.33a2.991,2.991,0,0,1,2.984,2.694,1.276,1.276,0,0,0,1.273,1.148h3.1A1,1,0,0,1,25.522,8.164Zm-11.936-1h4.828a3.3,3.3,0,0,1-.255-.944,1,1,0,0,0-.994-.9h-2.33a1,1,0,0,0-.994.9A3.3,3.3,0,0,1,13.586,7.164Zm1.007,15.151V13.8a1,1,0,0,0-2,0v8.519a1,1,0,0,0,2,0Zm4.814,0V13.8a1,1,0,0,0-2,0v8.519a1,1,0,0,0,2,0Z"></path></svg>
     </span> 
       </div>
       <div class="box_inner">
    <!-- Product Name -->
    <div class="input__field_form">
        <input
            class="input__field h6"
            type="text"
            id="ContactForm-zip-code"
            name="contact[product name]"
            value=""
            placeholder="Product Name"
        >
        <label class="visually-hidden" for="ContactForm-zip-code">Product Name</label>
    </div>
    <!-- Style Name -->
    <div class="input__field_form">
        <input
            class="input__field h6"
            type="text"
            id="ContactForm-zip-code"
            name="contact[style number]"
            value=""
            placeholder="Style Number"
        >
        <label class="visually-hidden" for="ContactForm-zip-code">style number</label>
    </div>

   <!-- Gender -->
    <div class="input__field_form">
      <select id="gender"   class="input__field" name="contact[gender]">
       <option value="gender">Gender</option>
       <option value="male">Male</option>
      <option value="female">Female</option>
      </select>
    </div>

  <!-- Color -->
    <div class="input__field_form">
        <input
            class="input__field h6"
            type="text"
            id="ContactForm-zip-code"
            name="contact[color]"
            value=""
            placeholder="Color"
        >
        <label class="visually-hidden" for="ContactForm-zip-code">Color</label>
    </div>
         
     <!-- quantity -->
    <div class="input__field_form">
        <input
            class="input__field h6"
            type="text"
            id="ContactForm-zip-code"
            name="contact[quantity]"
            value=""
            placeholder="Quantity"
        >
        <label class="visually-hidden" for="ContactForm-zip-code">Quantity</label>
    </div>
    <!-- <!-- Existing Phone Field -->
     
           
         
    <div class="customize">
      <h4>Customize</h4>
        <input
            class="h6"
            type="checkbox"
           
            name="contact[names]"
            value="yes"
            placeholder="Names"
        >
        <label class="">Names</label>

        <input
            class=" h6"
            type="checkbox"
          
            name="contact[front number]"
            value="yes"
            placeholder="Front Number"
        >
        <label class="" >Front Number</label>

          <input
            class=" h6"
            type="checkbox"
         
            name="contact[back number]"
            value="yes"
            placeholder="Back Number"
        >
        <label class="">Back Number</label>

          <input
            class=" h6"
            type="checkbox"
           
            name="contact[chest logo]"
            value="yes"
            placeholder="Chest Logo"
        >
        <label class="" >Chest logo</label>

          <input
            class=" h6"
            type="checkbox"
          
            name="contact[crest logo]"
            value="yes"
            placeholder="crest logo"
        >
        <label class="">Crest logo</label>
    
      
     </div> 
    </div>     
   </div>
    <div id="additionalProductBoxes"></div>

    <span id="add_another_product">Add another product</span>
<script>
    document.addEventListener("DOMContentLoaded", function () {
        let productCount = 2;

        function createProductBox() {
            const productBox = document.querySelector(".product_box");

            if (!productBox) {
                console.error("Product box template not found");
                return;
            }

            const newProductBox = productBox.cloneNode(true);
            newProductBox.setAttribute("id", "product-box-" + productCount);

            // Update the item counter in the new product box
            const counterSpan = newProductBox.querySelector(".counter");
            if (counterSpan) {
                counterSpan.textContent = "Item " + productCount;
            }

            // Add event listener to the deleteBox
            const deleteBox = newProductBox.querySelector(".deleteBox");
            if (deleteBox) {
                deleteBox.addEventListener("click", function () {
                    // Delete the relevant box
                    newProductBox.parentNode.removeChild(newProductBox);
                });
            }

            // Update input and select elements
            newProductBox.querySelectorAll("input, select").forEach(function (input) {
                const nameAttribute = input.getAttribute("name");
                if (nameAttribute) {
                    const updatedName = nameAttribute.replace(/\[([^\]]*)\]/, function(match, innerContent) {
                        return "[" + innerContent + productCount + "]";
                    });
                    console.log("Updating input name:", nameAttribute, "to", updatedName);
                    input.setAttribute("name", updatedName);

                    // Update input id
                    const inputId = input.getAttribute("id");
                    if (inputId) {
                        const updatedId = inputId.replace(/\d+/, productCount);
                        console.log("Updating input id:", inputId, "to", updatedId);
                        input.setAttribute("id", updatedId);
                    }

                    // Update label for attribute
                    const labelFor = input.getAttribute("id");
                    const labelElement = document.querySelector('label[for="' + labelFor + '"]');
                    if (labelElement) {
                        const updatedLabelFor = labelFor.replace(/\d+/, productCount);
                        console.log("Updating label for:", labelFor, "to", updatedLabelFor);
                        labelElement.setAttribute("for", updatedLabelFor);
                    }

                    // Copy input value
                    input.value = input.value;
                }
            });

            productBox.parentNode.insertBefore(newProductBox, document.getElementById("add_another_product"));
            productCount++;
        }

        document.getElementById("add_another_product").addEventListener("click", function () {
            createProductBox();
        });
    });
</script>

    <!-- Existing Textarea Field -->
    <div class="input__field_form textarea__field">
        <textarea
            rows="10"
            id="ContactForm-body"
            class="text__area input__field h6"
            name="contact[{{ 'templates.contact.form.comment' | t }}]"
            placeholder="Additional comments"
        >
            {{- form.body -}}
        </textarea>
        <label class="visually-hidden field__label" for="ContactForm-body">
            {{- 'templates.contact.form.comment' | t -}}
        </label>
    </div>

    <!-- Existing Submit Button -->
    <div class="contact__button">
        <button type="submit" class="{{ button_class }}">
            {{ 'templates.contact.form.send' | t }}
            {%- if section.settings.button_type == 'icon' -%}
                {% render 'icon-arrow' %}
            {%- endif -%}
        </button>
    </div>
{%- endform -%}
        </div>
      </div>
    </div>
  </div>
</div>





{% schema %}
{
  "name": "Request Quote form",
  "tag": "section",
  "disabled_on": {
      "groups": ["header"]
    },
  "class": "spaced-section",
  "settings": [
	{
      "type": "text",
      "id": "heading",
      "default": "Contact form",
      "label": "Heading"
    },
    {
      "type": "select",
      "id": "heading_tag",
      "options": [
        {
          "value": "h2",
          "label": "h2"
        },
        {
          "value": "h1",
          "label": "h1"
        }
      ],
      "default": "h2",
      "label": "Heading tag",
      "info": "Used for accessibility and SEO"
    },
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        {
          "value": "h2",
          "label": "Small"
        },
        {
          "value": "h1",
          "label": "Medium"
        },
        {
          "value": "h0",
          "label": "Large"
        }
      ],
      "default": "h1",
      "label": "Heading size"
    },
    {
      "type": "header",
      "content": "Contact form wrapper"
    },
	{
      "type": "checkbox",
      "id": "full_width",
      "label": "Make section full width",
      "default": false
    },
    {
      "type": "text",
      "id": "form_heading",
      "default": "You can contact us anytime.",
      "label": "Heading"
    },
    {
      "type": "select",
      "id": "form_heading_tag",
      "options": [
        {
          "value": "h2",
          "label": "h2"
        },
        {
          "value": "h3",
          "label": "h3"
        }
      ],
      "default": "h3",
      "label": "Heading tag",
      "info": "Used for accessibility and SEO"
    },
    {
      "type": "select",
      "id": "form_heading_size",
      "options": [
        {
          "value": "h4",
          "label": "Small"
        },
        {
          "value": "h3",
          "label": "Medium"
        },
        {
          "value": "h2",
          "label": "Large"
        }
      ],
      "default": "h4",
      "label": "Heading size"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "default": "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed urna accumsan odio odio.",
      "label": "Subheading"
    },
    {
      "type": "select",
      "id": "button_type",
      "label": "Button type",
      "default": "primary",
      "options": [
		{
          "value": "primary",
          "label": "Primary button"
        },
        {
          "value": "secondary",
          "label": "Secondary button"
        },
		{
          "value": "icon",
          "label": "Icon button"
        },
		{
          "value": "underline",
          "label": "Underline button"
        }
      ]
    },
	  {
          "type": "select",
          "id": "color_scheme",
          "options": [
            {
              "value": "accent-1",
              "label": "t:sections.header.settings.color_scheme.options__1.label"
            },
            {
              "value": "accent-2",
              "label": "t:sections.header.settings.color_scheme.options__2.label"
            },
            {
              "value": "background-1",
              "label": "t:sections.header.settings.color_scheme.options__3.label"
            },
            {
              "value": "background-2",
              "label": "t:sections.header.settings.color_scheme.options__4.label"
            },
            {
              "value": "background-3",
              "label": "Background 3"
            },
            {
              "value": "inverse",
              "label": "t:sections.header.settings.color_scheme.options__5.label"
            }
          ],
          "default": "background-3",
          "label": "t:sections.header.settings.color_scheme.label"
    } ,
     {
      "type": "header",
      "content": "Section padding"
    },
	  {
          "type": "paragraph",
          "content": "Desktop"
        },
        {
          "type": "range",
          "id": "padding_top",
          "min": 0,
          "max": 150,
          "step": 5,
          "unit": "px",
          "label": "Padding top",
          "default": 0
        },
        {
          "type": "range",
          "id": "padding_bottom",
          "min": 0,
          "max": 150,
          "step": 5,
          "unit": "px",
          "label": "Padding bottom",
          "default": 0
        },
		{
          "type": "paragraph",
          "content": "Mobile"
        },
		{
          "type": "range",
          "id": "mobile_padding_top",
          "min": 0,
          "max": 150,
          "step": 5,
          "unit": "px",
          "label": "Padding top",
          "default": 0
        },
        {
          "type": "range",
          "id": "mobile_padding_bottom",
          "min": 0,
          "max": 150,
          "step": 5,
          "unit": "px",
          "label": "Padding bottom",
          "default": 0
        }
  ],
  "blocks": [
        {
          "type": "contact_list",
          "name": "contact item",
          "settings": [
			      {
              "type": "text",
              "id": "heading",
              "default": "Heading:",
              "label": "Heading"
            },
            {
              "type": "richtext",
              "id": "text",
              "default": "<p>Share information such as the store's physical address, contact details, opening hours, etc.<p>",
              "label": "t:sections.rich-text.blocks.text.settings.text.label"
            }
          ]
        }
    ],
  "presets": [
    {
      "name": "Request Quote form",
      "blocks":[
        {
        	"type": "contact_list",
			 "settings": {
				"heading": "Phone:",
			    "text": "<p> <strong> (372) 587-2335 </strong> </br> (372) 555-4665</p>"
			  }
        },
        {
        	"type": "contact_list",
              "settings": {
              "heading": "Email:",
              "text": "<p><a href=\"mailto:info@demomeil.com\" target=\"_blank\" title=\"mailto:info@demomeil.com\"><strong>info@demomeil.com<\/strong><\/a><\/p><p><a href=\"mailto:company@demo.com\" target=\"_blank\" title=\"mailto:company@demo.com\"><strong>company@demo.com<\/strong><\/a><\/p>"
            }
        },
		{
        	"type": "contact_list",
              "settings": {
              "heading": "Address:",
            "text": "<p><strong>Iris Watson<br\/>P.O. Box 283 8562 Fusce Rd.<br\/>Frederick Nebraska 20620<\/strong><\/p>"
            }
        }
      ]
    }
  ]
}
{% endschema %}
